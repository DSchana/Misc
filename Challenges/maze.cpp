#include <cstdio>
#include <vector>

using namespace std;

int minMoves(vector < vector < int > > maze, int x, int y) {
	vector<int> toCheck_x;
	vector<int> toCheck_y;
	toCheck_x.push_back(0);
	toCheck_y.push_back(0);

	
}

int main() {
	return 0;
}
